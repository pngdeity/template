# Remote state backend — uncomment your provider and configure.
# Keeping state local (no backend block) is fine for solo development.

# terraform {
#   backend "gcs" {
#     bucket = "{{ repo_name }}-tfstate"
#   }
# }

# terraform {
#   backend "s3" {
#     bucket         = "{{ repo_name }}-tfstate"
#     key            = "terraform.tfstate"
#     region         = "us-east-1"
#     encrypt        = true
#     dynamodb_table = "terraform-lock"
#   }
# }

# terraform {
#   backend "pg" {
#     conn_str    = "postgres://user:pass@host:5432/tfstate?sslmode=require"
#     schema_name = "{{ repo_name }}_tfstate"
#   }
# }

# terraform {
#   backend "cloud" {
#     hostname     = "app.terraform.io"
#     organization = "{{ repo_name }}"
#     workspaces {
#       name = "production"
#     }
#   }
# }
