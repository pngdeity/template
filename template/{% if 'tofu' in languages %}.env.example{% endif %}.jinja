# Copy this file and fill in real values — never commit secrets to git.
#   cp .env.example .env
#   $EDITOR .env
#
# Source before running OpenTofu:
#   source .env && tofu init && tofu plan && tofu apply
#
# {% if tofu_context == 'app' %}
# Optionally pull outputs from the org infra repo:
#   tofu state pull -state=../org-infra/terraform.tfstate > /dev/null
# {% endif %}

OPENTOFU_VAR_example_required=
